import { API, VERSION } from "./base";

const BASE_PRODUCT: string = `${API}/${VERSION}/products`;
export const LIST_PRODUCT: string = BASE_PRODUCT;
export const CUD_PRODUCT: string = `${BASE_PRODUCT}/`;
